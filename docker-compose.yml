services:
  lab-sample-tracking:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/lab-sample-tracking
    environment:
      - HOST=0.0.0.0
      - PORT=80
    command: flask --app cultures --debug run --host 0.0.0.0 --port 80