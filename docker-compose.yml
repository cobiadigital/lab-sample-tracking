version: "3.9"

services:
  lab-sample-tracking:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/lab-sample-tracking
    environment:
      - HOST=0.0.0.0
      - PORT=5000
    command: flask --app cultures run --host $HOST --port $PORT